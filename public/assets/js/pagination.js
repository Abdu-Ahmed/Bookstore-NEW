document.addEventListener('DOMContentLoaded', function() {
    var carousel = new bootstrap.Carousel(document.getElementById('booksCarousel'), {
        interval: 3000,
        wrap: true
    });
});
